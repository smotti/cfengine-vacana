bundle common va_processes
{
  vars:

    "inputs"
      slist => {
        "${this.promise_dirname}/processes/process_check.cf",
      };
}

body file control
{
  inputs => { "@{va_processes.inputs}" };
}
